[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agt-ao-exact"
version = "0.1.0"
description = "Higher-order AO implementation for AGT^AO reproduction"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
